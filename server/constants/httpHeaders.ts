export enum HttpContentType {
    Json = 'application/json',
    Html = 'text/html'
}

export enum HttpContentTypeCharset {
    Utf8 = 'charset=utf8'
}

export enum HttpHeaderName {
    Accept = 'Accept',
    ContentType = 'Content-Type',
    Authorization = 'Authorization'
}
